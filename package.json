{
  "name": "ocwi-loader",
  "version": "1.0.1",
  "description": "Stable loader for the OCWI embeddable widget runtime.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "node scripts/build.mjs",
    "test": "npm run build && node test/loader.test.mjs"
  },
  "ocwiLoader": {
    "corePackage": "ocwi-core",
    "coreVersion": "latest",
    "coreFile": "dist/ocwi.min.js",
    "cdnBase": "https://cdn.jsdelivr.net/npm"
  }
}
